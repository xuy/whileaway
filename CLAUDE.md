# Project instructions

## Linear

For any Linear work in this repo (tickets, issues, projects, comments), use the
**`whileaway`** MCP server — tools prefixed `mcp__whileaway__*`. This repo's tracker
is the whileaway workspace (team WHI).

Do **not** use the `noa` server (the NOA/Onnoah workspace) for this repo unless I
explicitly ask for it.
